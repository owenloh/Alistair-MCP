{
  "skill": "notion-master",
  "name": "notion-master",
  "type": "skill-description",
  "description": "Load this FIRST for ANY Notion operation. This is the parent skill governing all Notion access (reads and writes) in Owen's workspace - projects, actions, areas, references, databases, views, Next actions, Someday items, statuses, Directions, page edits, the Briefing page, the references tray. It holds the read decision tree (connector vs token script vs search), the PARA ID registry, and the mandatory safe-write protocol that protects against accidental data loss. Every other skill that touches Notion (daily-brief, notion-references-tray) depends on the rules here; the model must load this skill and apply it before making any Notion tool call.",
  "instructions": "# Notion Master (load first for all Notion access)\n\nThis is the parent skill for ALL Notion operations. Load it and apply it before any Notion tool call, for reads AND writes.\n\n**Why this exists:** (a) the Notion connector behaves non-obviously and rediscovering its limits each session wastes time; (b) Owen's Notion is sacred — a careless write that deletes content is unacceptable, so the safe-write protocol lives here as the single source of truth.\n\n**How this skill relates to the others (important):** Skills do not auto-load each other. A skill saying \"see notion-master\" does not pull this file in by itself; the model has to load this skill as its own step. What makes that reliable is this skill's own description (it triggers on any Notion operation) plus the standing memory instruction to load it first. Other Notion skills (daily-brief, notion-references-tray) carry only their unique details and rely on the rules here; they do not duplicate the protocol.\n\n**Division of responsibility:**\n- This skill: the decision tree, the ID registry, connector-vs-token-script routing for reads, AND the safe-write protocol for all writes.\n- `notion-references-tray`: only tray-specific details (which page, where in it, entry format). Defers here for the protocol.\n- `daily-brief` and any other skill: reference this skill for all Notion operations; never re-implement routing or the protocol.\n\n---\n\n## Decision tree (apply before every Notion operation)\n\n### Step 1: Is this a WRITE (create, edit, append, update properties)?\nFollow the **Safe-write protocol** below. Always. If the write is a tray append, also read `notion-references-tray` for placement, but the protocol here governs.\n\n### Step 2: Is this a read of a KNOWN INDIVIDUAL PAGE (you have the exact page ID)?\nUse `notion_fetch` with the page ID. The connector handles this reliably. No token script needed.\n\n### Step 3: Is this a FILTERED QUERY (get all rows where status = X)?\n**Use `notion_query_database`** with an explicit `filter` object — it queries the database directly by property (the saved `view://` URLs are not readable over the API). For the standard PARA reads you can also call `daily_brief` or `load_context`, which return them already filtered.\n\n### Step 4: Is this a SCHEMA READ (database structure, view filter definitions)?\nUse `notion_fetch` on the `collection://...` URL or the database page URL. Returns schema and view definitions (not the rows). No token script needed.\n\n### Step 5: Is this a SEMANTIC/FUZZY SEARCH across the workspace or a data source?\nUse `notion_search`. It returns real database rows ranked by semantic relevance, capped at 25, and cannot filter by property value. Returned pages can then be `notion_fetch`ed individually for their properties. Use it to find a specific page you know exists, or as a best-effort partial read when the token script is unavailable. Always label search-derived results as \"best-effort, not a complete filtered list.\"\n\n---\n\n## Safe-write protocol (MANDATORY for every Notion write)\n\nOwen has lost content to careless writes before. This is not about the connector being unreliable — it executes faithfully, which is exactly why a bad instruction is dangerous. These steps are not optional, no matter how small the edit looks.\n\n1. **Read first, save the before-state.** `notion_fetch` the target page and keep the full result as the before-state snapshot. Never write to a page you have not just fetched.\n2. **Check.** Confirm it is the right page (right title / ID / expected anchor content present) and locate the exact insertion or edit point.\n3. **Write, insert/append/targeted-edit only.** Use `notion_update_page` with `command=update_content` and `content_updates=[{old_str, new_str}]` (targeted search-replace), or `command=insert_content` with `content`. Anchor `old_str` on unique existing text. To add, include that anchor in `new_str` so nothing is dropped. To delete a specific entry/line/block, set `new_str` shorter than `old_str` (or empty) so only that span is removed — targeted deletion is allowed and expected when Owen asks for it.\n   - **NEVER use `replace_content`.** It overwrites the ENTIRE page with the string you provide; anything missing from that string is permanently deleted. This is the single biggest accidental-deletion risk. The point is not \"never delete\" — targeted deletion via `update_content` is fine — it is \"never overwrite the whole page in one call.\" Only ever use `replace_content` if Owen explicitly instructs it in this conversation.\n4. **Read again, confirm only the intended thing changed.** `notion_fetch` the page again and diff against the before-state: the intended change is present (or the intended span is gone), and nothing else moved or vanished. If anything outside the intended change is altered or missing, STOP, report, and restore from the before-state snapshot.\n\n**On a write timeout:** Notion writes sometimes return `RequestTimeoutError` even when they actually landed. Do not blindly retry (risks duplicates) and do not blindly assume success (risks silent loss). Instead, **re-fetch the page and check whether the write is present, then decide**: if it is there, the write succeeded — done; if it is not, retry the write once and check again. Verification drives the decision, not the error message. (A `No approval received` error is different: the write did not execute and Owen needs to approve it.)\n\n**Math in Notion:** use the native inline form `` $`...`$ `` (dollar-backtick-LaTeX-backtick-dollar), not plain `$...$` (which silently fails near **bold**). Keep equations out of bold lead-ins.\n\n---\n\n## Filtered reads (`notion_query_database`)\n\nCall `notion_query_database` with the database id and an explicit `filter` object for any property-filtered read. The composite tools `daily_brief` and `load_context` already return the standard PARA-filtered lists, so prefer them for the brief.\n\nThe standard filtered reads return: ACTIVE PROJECTS, NEXT ACTIONS (Action Status = Next, project Active/Complete or project-less), SOMEDAY PROJECTS, SOMEDAY ACTIONS (Action Status = Someday).\n\n**Use for:** any filtered read (Next actions, Someday items, Active projects). Authoritative, complete, status-correct.\n**Do not use for:** individual page reads (`notion_fetch` is faster), schema reads (`notion_fetch` returns them), writes (use the safe-write protocol).\n**If a read returns nothing or errors:** fall back to `notion_search` (semantic, capped, not status-filtered) with an honest label. Never fabricate; the `view://` URLs 400 every time.\n\n---\n\n## PARA registry (IDs)\n\n- **Areas DB**: `collection://54816fca-6f6c-4588-8c1a-1cdfcc6c9092` (Status: Active/Paused/Retired)\n- **Projects DB**: `collection://f0ea8841-ca74-47b7-a28a-0b367bca8c41` — database page: `https://app.notion.com/p/b9c0cd8cfa6c46d195ed87d7ef97d971`\n  - \"Active\" view: `view://37e6f0cc-dd76-8053-adb3-000ca18ab680`\n  - \"Someday\" view: `view://37f6f0cc-dd76-803e-9757-000cd865d053`\n  - \"All by Status\" view: `view://2b375686-86af-412d-852b-17cc10264c52`\n- **Actions DB**: `collection://1d3eb1dd-2803-4692-a4d5-6ca9709ae570` — database page: `https://app.notion.com/p/2ebc58c5861747488021fcc2a37d3a97`\n  - \"Next Actions\": `view://37f6f0cc-dd76-80c1-accb-000cc0fbbbd7`\n  - \"NP Next\": `view://37f6f0cc-dd76-8005-9d54-000c0b3f1a39`\n  - \"Waiting\": `view://37f6f0cc-dd76-800c-ad94-000c9a9c4b64`\n  - \"Someday\": `view://37f6f0cc-dd76-803f-b1d3-000c0f34607a`\n  - \"All by Status\": `view://ee547a67-b3fe-4c03-a55d-108f209d3c19`\n- **Library hub**: `1fa6f0cc-dd76-809e-8bcb-e5db5ae28237`\n- **References Tray** (\"Unorganised References\"): `37e6f0cc-dd76-8086-a07d-f6704b0c25df`\n- **Briefing page** (\"Alistair's Brief\"): `3806f0cc-dd76-80bb-9e16-fcce720de5ee` (under Mission Control `34f6f0cc-dd76-801d-b0ec-de6c10685d10`)\n\nA **Next action** = an Action where Action Status = `Next` AND its project is `Active` or `Complete`, OR it has no project (completion is now Action Status `Done`, which is excluded). Three kinds qualify: actions under an Active project, actions under a Complete project, and project-less actions. Actions under Someday/Dropped/Paused projects are NOT Next actions. Next actions have no due dates by design.\n\nA **Someday item** = Status/Action Status = `Someday`.\n\nProject Status values are `Active` / `Someday` / `Complete` / `Dropped` (completed projects are tagged `Complete`, never \"Completed\" or \"Done\"). These definitions are canonical for ALL Notion access, not just the brief.\n\n---\n\n## Connector capability map (verified)\n\n| Operation | Tool | Works? |\n|-----------|------|--------|\n| Read individual page by ID | `notion_fetch` | Yes |\n| Read database schema + view definitions | `notion_fetch` on collection:// or DB URL | Yes (schema only, no rows) |\n| Read rows of a view | `notion_fetch` on `view://...` | No — 400 error every time |\n| Filter rows by property (Status = X) | `notion_query_database` (explicit filter) | Yes |\n| Semantic search across workspace | `notion_search` | Yes — relevance-ranked, capped at 25 |\n| Semantic search within a data source | `notion_search` with `data_source_url` | Yes — returns real rows, semantic only |\n| Write / update pages | `notion_update_page` | Yes — always via the safe-write protocol above |\n| Create views with filters | not exposed by the Notion API | No — 501; use `notion_query_database` |\n\n## Honest-failure rule\n\nIf the token script is unavailable and semantic search is the only option: say so, search with an honest label, never fabricate statuses or counts. A clear \"best-effort, top 25 semantic matches\" always beats a confident wrong list.\n",
  "sections": [
    {
      "heading": "Decision tree (apply before every Notion operation)",
      "body": ""
    },
    {
      "heading": "Step 1: Is this a WRITE (create, edit, append, update properties)?",
      "body": "Follow the **Safe-write protocol** below. Always. If the write is a tray append, also read `notion-references-tray` for placement, but the protocol here governs."
    },
    {
      "heading": "Step 2: Is this a read of a KNOWN INDIVIDUAL PAGE (you have the exact page ID)?",
      "body": "Use `notion_fetch` with the page ID. The connector handles this reliably. No token script needed."
    },
    {
      "heading": "Step 3: Is this a FILTERED QUERY (get all rows where status = X)?",
      "body": "**Use `notion_query_database`** with an explicit `filter` object — it queries the database directly by property (the saved `view://` URLs are not readable over the API). For the standard PARA reads you can also call `daily_brief` or `load_context`, which return them already filtered."
    },
    {
      "heading": "Step 4: Is this a SCHEMA READ (database structure, view filter definitions)?",
      "body": "Use `notion_fetch` on the `collection://...` URL or the database page URL. Returns schema and view definitions (not the rows). No token script needed."
    },
    {
      "heading": "Step 5: Is this a SEMANTIC/FUZZY SEARCH across the workspace or a data source?",
      "body": "Use `notion_search`. It returns real database rows ranked by semantic relevance, capped at 25, and cannot filter by property value. Returned pages can then be `notion_fetch`ed individually for their properties. Use it to find a specific page you know exists, or as a best-effort partial read when the token script is unavailable. Always label search-derived results as \"best-effort, not a complete filtered list.\"\n\n---"
    },
    {
      "heading": "Safe-write protocol (MANDATORY for every Notion write)",
      "body": "Owen has lost content to careless writes before. This is not about the connector being unreliable — it executes faithfully, which is exactly why a bad instruction is dangerous. These steps are not optional, no matter how small the edit looks.\n\n1. **Read first, save the before-state.** `notion_fetch` the target page and keep the full result as the before-state snapshot. Never write to a page you have not just fetched.\n2. **Check.** Confirm it is the right page (right title / ID / expected anchor content present) and locate the exact insertion or edit point.\n3. **Write, insert/append/targeted-edit only.** Use `notion_update_page` with `command=update_content` and `content_updates=[{old_str, new_str}]` (targeted search-replace), or `command=insert_content` with `content`. Anchor `old_str` on unique existing text. To add, include that anchor in `new_str` so nothing is dropped. To delete a specific entry/line/block, set `new_str` shorter than `old_str` (or empty) so only that span is removed — targeted deletion is allowed and expected when Owen asks for it.\n   - **NEVER use `replace_content`.** It overwrites the ENTIRE page with the string you provide; anything missing from that string is permanently deleted. This is the single biggest accidental-deletion risk. The point is not \"never delete\" — targeted deletion via `update_content` is fine — it is \"never overwrite the whole page in one call.\" Only ever use `replace_content` if Owen explicitly instructs it in this conversation.\n4. **Read again, confirm only the intended thing changed.** `notion_fetch` the page again and diff against the before-state: the intended change is present (or the intended span is gone), and nothing else moved or vanished. If anything outside the intended change is altered or missing, STOP, report, and restore from the before-state snapshot.\n\n**On a write timeout:** Notion writes sometimes return `RequestTimeoutError` even when they actually landed. Do not blindly retry (risks duplicates) and do not blindly assume success (risks silent loss). Instead, **re-fetch the page and check whether the write is present, then decide**: if it is there, the write succeeded — done; if it is not, retry the write once and check again. Verification drives the decision, not the error message. (A `No approval received` error is different: the write did not execute and Owen needs to approve it.)\n\n**Math in Notion:** use the native inline form `` $`...`$ `` (dollar-backtick-LaTeX-backtick-dollar), not plain `$...$` (which silently fails near **bold**). Keep equations out of bold lead-ins.\n\n---"
    },
    {
      "heading": "Token script (for all filtered reads)",
      "body": "Call `notion_query_database` with the database id and an explicit `filter` object for any property-filtered read. The composite tools `daily_brief` and `load_context` already return the standard PARA-filtered lists, so prefer them for the brief.\n\nThe standard filtered reads return: ACTIVE PROJECTS, NEXT ACTIONS (Action Status = Next, project Active/Complete or project-less), SOMEDAY PROJECTS, SOMEDAY ACTIONS (Action Status = Someday).\n\n**Use for:** any filtered read (Next actions, Someday items, Active projects). Authoritative, complete, status-correct.\n**Do not use for:** individual page reads (`notion_fetch` is faster), schema reads (`notion_fetch` returns them), writes (use the safe-write protocol).\n**If a read returns nothing or errors:** fall back to `notion_search` (semantic, capped, not status-filtered) with an honest label. Never fabricate; the `view://` URLs 400 every time.\n\n---"
    },
    {
      "heading": "PARA registry (IDs)",
      "body": "- **Areas DB**: `collection://54816fca-6f6c-4588-8c1a-1cdfcc6c9092` (Status: Active/Paused/Retired)\n- **Projects DB**: `collection://f0ea8841-ca74-47b7-a28a-0b367bca8c41` — database page: `https://app.notion.com/p/b9c0cd8cfa6c46d195ed87d7ef97d971`\n  - \"Active\" view: `view://37e6f0cc-dd76-8053-adb3-000ca18ab680`\n  - \"Someday\" view: `view://37f6f0cc-dd76-803e-9757-000cd865d053`\n  - \"All by Status\" view: `view://2b375686-86af-412d-852b-17cc10264c52`\n- **Actions DB**: `collection://1d3eb1dd-2803-4692-a4d5-6ca9709ae570` — database page: `https://app.notion.com/p/2ebc58c5861747488021fcc2a37d3a97`\n  - \"Next Actions\": `view://37f6f0cc-dd76-80c1-accb-000cc0fbbbd7`\n  - \"NP Next\": `view://37f6f0cc-dd76-8005-9d54-000c0b3f1a39`\n  - \"Waiting\": `view://37f6f0cc-dd76-800c-ad94-000c9a9c4b64`\n  - \"Someday\": `view://37f6f0cc-dd76-803f-b1d3-000c0f34607a`\n  - \"All by Status\": `view://ee547a67-b3fe-4c03-a55d-108f209d3c19`\n- **Library hub**: `1fa6f0cc-dd76-809e-8bcb-e5db5ae28237`\n- **References Tray** (\"Unorganised References\"): `37e6f0cc-dd76-8086-a07d-f6704b0c25df`\n- **Briefing page** (\"Alistair's Brief\"): `3806f0cc-dd76-80bb-9e16-fcce720de5ee` (under Mission Control `34f6f0cc-dd76-801d-b0ec-de6c10685d10`)\n\nA **Next action** = an Action where Action Status = `Next` AND its project is `Active` or `Complete`, OR it has no project (completion is now Action Status `Done`, which is excluded). Three kinds qualify: actions under an Active project, actions under a Complete project, and project-less actions. Actions under Someday/Dropped/Paused projects are NOT Next actions. Next actions have no due dates by design.\n\nA **Someday item** = Status/Action Status = `Someday`.\n\nProject Status values are `Active` / `Someday` / `Complete` / `Dropped` (completed projects are tagged `Complete`, never \"Completed\" or \"Done\"). These definitions are canonical for ALL Notion access, not just the brief.\n\n---"
    },
    {
      "heading": "Connector capability map (verified)",
      "body": "| Operation | Tool | Works? |\n|-----------|------|--------|\n| Read individual page by ID | `notion_fetch` | Yes |\n| Read database schema + view definitions | `notion_fetch` on collection:// or DB URL | Yes (schema only, no rows) |\n| Read rows of a view | `notion_fetch` on `view://...` | No — 400 error every time |\n| Filter rows by property (Status = X) | `notion_query_database` (explicit filter) | Yes |\n| Semantic search across workspace | `notion_search` | Yes — relevance-ranked, capped at 25 |\n| Semantic search within a data source | `notion_search` with `data_source_url` | Yes — returns real rows, semantic only |\n| Write / update pages | `notion_update_page` | Yes — always via the safe-write protocol above |\n| Create views with filters | not exposed by the Notion API | No — 501; use `notion_query_database` |"
    },
    {
      "heading": "Honest-failure rule",
      "body": "If the token script is unavailable and semantic search is the only option: say so, search with an honest label, never fabricate statuses or counts. A clear \"best-effort, top 25 semantic matches\" always beats a confident wrong list."
    }
  ]
}
